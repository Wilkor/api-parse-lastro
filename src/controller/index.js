const { receive, receiveLastro, configLastro,getConfigLastro,ImageImob
} = require('./ReceiveMessage');

module.exports = {
    receive,
    receiveLastro,
    configLastro,
    getConfigLastro,
    ImageImob
}